#include "./neurons.h"
#include <stdlib.h>

/*
double output(struct Perceptron *per) {
  double result = 0;
  size_t i = 0;

  while (i < per->num_entries) {
    result += per->entries[i] * per->biases[i];
    i++;
  }

  return result;
}
*/
